# Master C++

**c++** is a high-level, general-purpose programming language , first released was in 1985 as an extention of **C** programming language, adding object-oriented (OOP) features.

## Module 00 :

Understand float point and make new variable type that work like float but way faster .


### ex00:

> *fixedpoint* : create a new data type that hold float point and store it in in raw int format .

### ex01:

> *fixedpoint* : set and retrive the value of fixedPoint in integer and decimal format .

### ex02:

> *fixedpoint* : add overload operation on fixed point (add, sub, mul, div, pre/pos inc, ...).

### ex03:

> *fixedpoint* : use my new data type to preform checks if a point is within a triangle .
